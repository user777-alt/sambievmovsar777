```
# ALT-NET
```bash
apt-get update && apt-get dist-upgrade -y
```
```bash
nano /etc/sudoers
root ALL=(ALL:ALL) ALL - убрать решетку
user ALL=(ALL:ALL) ALL
```
```bash
apt-get install ahttpd alterator alterator fbi -y
```
```bash
systemctl enable --now alteratord
systemctl enable --now ahttpd
```
```bash
apt-get install alterator-dhcp alterator-bind alterator-net-iptables -y
```
```bash
reboot
```
```bash
Заходим в браузер и пишем свой ip:8080
root
password
```
Серверы
```bash
DHSP-сервер
Начальный ip адрес:192.168.20.10
Конечный ip адрес:192.168.20.20
DNS-сервер:192.168.20.2
Домен поиска:8.8.8.8
Шлюз по умолчанию:192.168.20.2
Применить
```
Брандмауэр
```bash
Внешние сети
Выберите внешние интерфейсы:ens33
Включить брандмауэр:поставь галочку
Применить
```
Система
```bash
Выключение компьютера
Перезагрузить компьютер сейчас
Применить
```
ALT-CLI
```bash
Настраиваем ALT-server авто-установка задаем время Екатиринбург и дальше как обычно устанавливаем ALT-server
задаем пароль:
root:2222
user:1111
```
Проверка
```bash
ip -c -br a
```
```bash
echo "default via 172.16.1.1" > /etc/net/ifaces/ens33/ipv4route 
```
```bash
nano /etc/net/ifaces/ens33/resolv.conf
name_server=127.0.0.1
name_server=192.168.0.0-Начальный ip адрес DHCP
```
```bash
resolvconf -u
systemctl restart network
```
Проверка
```bash
ping <ip>                        #ALT-NET
ping 8.8.8.8
apt-get update
```
```bash
```
```bash
```
```bash
```
```bash
```
```bash
```
```bash
```
```bash
